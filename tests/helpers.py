"""Path-shortening import convention (no auth infrastructure remains).

The API is unauthenticated: no users are seeded, no bearer tokens are issued,
and no client carries Authorization headers anywhere in the test suite.
"""

# (intentionally empty)